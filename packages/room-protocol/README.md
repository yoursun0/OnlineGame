# room-protocol

Shared room events, room-code formatting, guest membership payloads, and versioned move envelopes belong here.

Game prefixes are data owned by each game registry; this package provides the common validation shape.

