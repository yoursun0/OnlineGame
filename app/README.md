# Production web app

This folder is reserved for the production Playroom web app that will replace the static prototype.

Expected responsibilities:

- public game catalogue;
- create/join room routes;
- room waiting state;
- game shell and game-specific screen mounting;
- Vercel deployment entry point.

The current runnable artifact remains `prototype/index.html` until the production framework is introduced deliberately.

